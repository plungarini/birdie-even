[Unit]
Description=BirdNET FastAPI Server
After=network.target

[Service]
User=root
WorkingDirectory=/opt/birdnet
EnvironmentFile=/opt/birdnet/.env
ExecStart=/opt/birdnet/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8080
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target